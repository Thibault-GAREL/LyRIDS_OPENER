% TODO: write the abstract (≈150–250 words).
% Suggested structure:
%   1. Problem: closed-vocabulary NER cannot handle new entity types without retraining.
%   2. Gap in prior work: existing open NER methods either train a dedicated encoder
%      (Triplet Loss + K-means) or rely solely on prompting LLMs.
%   3. Proposed method: Opener combines a frozen mention detector (GLiNER),
%      a Matryoshka-truncatable embedding space (Nomic v1.5), and a per-label
%      Gaussian Mixture Model initialized on anchor words.
%   4. Key results: (placeholder) competitive performance against closed/open
%      baselines on [datasets], with native OOD detection and post-hoc hierarchy inference.
%   5. Take-away: a pre-trained embedding space is already discriminative enough for
%      semi-supervised entity typing — no encoder retraining required.

This paper introduces \textbf{Opener}, an open Named Entity Recognition pipeline that combines a frozen mention detector, a Matryoshka-truncatable embedding space, and per-label Gaussian Mixture Models initialized on anchor words. \emph{[Placeholder — to be rewritten once results are in.]}

Need to be updated with actual methods and results.
