\section{Conclusion}
\label{sec:conclusion}

% TODO: write the conclusion (≈0.5 to 1 page).
% Suggested structure:
%   - Recap of contributions (one short paragraph).
%   - Practical implications: open-NER without encoder retraining is feasible
%     when the pre-trained embedding space is already discriminative.
%   - Limitations:
%       * End-to-end quality is capped by the detector's recall on specialised domains
%         (the shared bottleneck), not by the typing head.
%       * Supervised typing needs target-domain labels; the zero-shot variant (OPENER-ZS)
%         depends on the quality of the label names.
%       * A single seed; energy measured on one machine.
%   - Future work:
%       * Improving mention detection on specialised domains.
%       * Visualization of the contrastive embedding space (UMAP / t-SNE).
%       * Extension beyond NER (entity linking, relation extraction).

\emph{[Placeholder - conclusion to be written.]}
